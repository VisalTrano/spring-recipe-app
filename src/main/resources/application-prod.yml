spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_prod?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
    username: visaltrano
    password: visal@123
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false